# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
dist/
build/
*.tsbuildinfo

# Plugin distribution: MCP server dist/ MUST ship in the repo so plugin caches
# (which don't run build steps) can launch the servers via ${CLAUDE_PLUGIN_ROOT}.
!/harness/dist/
!/ontology/dist/

# SQLite
*.sqlite
*.sqlite-journal
*.sqlite-shm
*.sqlite-wal
memory/

# IDEs
.vscode/
.idea/
.DS_Store

# Environment
.env
.env.*
!.env.example

# OAA runtime outputs (if generated)
# AGENTS.md
# ONTOLOGY.md
# agents.lock
